{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && (npx prisma migrate deploy || echo 'AVISO: no se pudo aplicar la migracion (falta configurar DATABASE_URL/DIRECT_URL en Vercel). El sitio se despliega igual; login y registro no funcionaran hasta configurar la base de datos.') && next build",
  "installCommand": "npm install"
}
